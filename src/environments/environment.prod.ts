export const environment = {
  production: true,
  groqApiKey: '', // proveer via variable de entorno en el proceso de build
  supabaseUrl: '', // proveer via variable de entorno en el proceso de build
  supabaseKey: '', // proveer via variable de entorno en el proceso de build
};
