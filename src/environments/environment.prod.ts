export const environment = {
  production:  true,
  supabaseUrl: '', // set via build environment variable
  supabaseKey: '', // set via build environment variable
  // AI API keys live in Supabase secrets — never in the build artifact.
};
